/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,
} from 'react-native';

import WebView from './webView';
AppRegistry.registerComponent('RNH5', () => WebView);
